// Re-exports for convenience. Canonical sources:
//   FrameFeatures        → ../utils/visAudioHelpers
//   MidiFeatures, MidiNote, InputData → ../utils/visMidiHelpers
export type { FrameFeatures } from '../utils/visAudioHelpers';
export type { MidiFeatures, MidiNote, InputData } from '../utils/visMidiHelpers';
